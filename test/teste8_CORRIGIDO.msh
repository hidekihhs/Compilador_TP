app _Teste8
%teste implementado pelo grupo
   integer a, b, c;
   start
     read (a);
     read (b);
     read (c);
     if ( a>=c || a<b ) then
        c := a + b;
     else
       if (a != 0) then
          c := b / a;
       else
          c := b * a; 
       end
    end;
    write(c);
  stop


